## Voice + prompt = personality
- Match phraasting to voice
- Dialect awareness 
- Emotional control (Add subtle directional in system messages but don't overdo)
- Keep replies brief

### Modify the TTS configuration to match the new personality.

## Change the voice according to our needs and dialect
- via this - https://docs.livekit.io/agents/models/tts/

## For outages and fallback suppose one TTS, LLM or any voice plugin stopped working so for that we need a fallback, so we can use livekit fallback adapters.